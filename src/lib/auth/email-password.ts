/**
 * Local email/password sign-in (this app's Better Auth DB — not the broker).
 *
 * Enabled so Brave / mobile users can sign in without Google/X OAuth
 * (Shields often block third-party OAuth redirects).
 */
export const emailAndPasswordEnabled = true;
