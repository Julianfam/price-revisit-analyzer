/**
 * Pro tools entry — Quantum Agent is the main Pro surface.
 * Kept as a thin re-export so older imports keep working.
 */
export { QuantumAgentPanel as ProToolsBar } from "@/components/quantum-agent";
